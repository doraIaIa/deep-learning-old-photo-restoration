# Training Scripts

This directory is reserved for training documentation and future training entrypoints when external data paths, manifests, and runtime dependencies are configured.

Training runs are documented through manifests. Full training entrypoints can be added here when external data paths are configured and claim-safe runtime boundaries remain clear.
