# Artifact Helper Scripts

This directory is reserved for checkpoint and artifact inspection helpers that summarize local availability, provenance, and verification state.

Root user-facing commands remain available at the top level, while this directory documents the intended grouping for artifact-focused utilities.
