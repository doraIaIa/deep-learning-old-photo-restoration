from __future__ import annotations

import numpy as np


def refine_mask_repair_wide(mask: np.ndarray) -> np.ndarray:
    """Placeholder cho bước refine mask.

    Hiện tại trả nguyên mask để giữ contract rõ ràng mà không giả triển khai thuật toán.
    """

    return mask
