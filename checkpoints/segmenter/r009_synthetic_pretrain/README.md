# R009 Synthetic Pretrain Checkpoint

This directory is the intended public-facing location for the synthetic-pretraining or initialization checkpoint if that artifact is made available locally.

The checkpoint binary is not committed to the repository. Its role is initialization before real-domain fine-tuning and controlled reproduction.

Use `artifacts/manifests/checkpoints_manifest.csv` to verify lineage, SHA256, and external source details.
