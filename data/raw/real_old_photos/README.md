# Real Old Photo Sources

This directory represents the expected local layout for real old photo datasets used for fine-tuning and evaluation.

Full datasets are external and intentionally excluded from Git. The repository stores manifests, split metadata, and small examples only.

Use `data/manifests/` and `artifacts/manifests/` to map local paths and run lineage.
