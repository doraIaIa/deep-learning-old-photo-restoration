# Synthetic R009 Split Metadata

This directory represents the expected location for split metadata related to the synthetic pretraining stage associated with R009.

The repository stores split metadata and small text artifacts only. Full datasets remain external and are intentionally excluded from Git.

Use `data/manifests/synthetic_crack3d_manifest.csv` and `artifacts/manifests/reproduction_runs_manifest.csv` to interpret dataset-to-run linkage.
