# Processed Data Layout

This directory documents the expected local layout for processed datasets and derived assets.

Processed training binaries and full image collections are intentionally excluded from Git. The repository stores metadata, manifests, split definitions, and small documentation assets only.

Use `data/manifests/` for dataset lineage and `artifacts/manifests/` for run-level linkage.
