# Real Fine-Tune Pairs

This directory represents processed real-domain fine-tuning pairs used by controlled Module 1 training and evaluation workflows.

Full fine-tuning datasets are external and intentionally excluded from Git. The repository stores manifests, split metadata, and small examples only.

Use `data/manifests/` and `artifacts/manifests/` to map local paths and run lineage.
